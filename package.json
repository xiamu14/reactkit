{
  "name": "reactkit",
  "private": true,
  "scripts": {
    "setup": "corepack enable && cd packages/reactotron && yarn install && yarn build",
    "start": "cd apps/reactkit-native-macos && swift run ReactKit",
    "build": "npm run build:reactotron && npm run build:macos",
    "build:reactotron": "cd packages/reactotron && yarn build",
    "build:macos": "cd apps/reactkit-native-macos && swift build"
  }
}
